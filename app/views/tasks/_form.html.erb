<%= render('layouts/errors', :object => @task) %>

<%= form_for [@list, @task] do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <br>
  <%= f.label :done %>
  <%= f.check_box :done %>
  <p>
    <%= f.submit %>
  </p>
<% end %>
